import 'package:flutter/cupertino.dart';

class FlashcardTab extends StatelessWidget {
  const FlashcardTab({super.key});

  @override
  Widget build(BuildContext context) {
    return const Column(
      children: [Text("flashcard")],
    );
  }
}
